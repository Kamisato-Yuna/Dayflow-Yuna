# ChangeLog

## 1.15.0 (114) - 2026-07-06

- Renamed the distributed macOS application bundle to `Dayflow-Yuna.app` so it can be installed alongside `Dayflow.app` in Applications.
- Changed the Yuna build bundle identifier to `teleportlabs.com.DayflowYuna` so macOS permissions are tracked separately from `Dayflow.app`.
- Updated the release DMG defaults to produce `Dayflow-Yuna.dmg`.
- Bumped the marketing version to `1.15.0` and build number to `114`.
